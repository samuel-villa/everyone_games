export function GameCard({ game }) {
  return (
    <div className="card">
      <div className="card2">
        { game.name }
        { game.name }
        { game.name }
        { game.name }
      </div>
    </div>
  );
}
